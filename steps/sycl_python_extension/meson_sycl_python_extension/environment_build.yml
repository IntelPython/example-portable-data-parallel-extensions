# To use:
#
#   $ conda env create -f environment.yml  # can use `mamba` too
#   $ conda activate kde-ext
#
name: kde-ext
channels:
  - conda-forge
  - intel
dependencies:
  - python
  - cython
  - scikit-build
  - cmake
  - numpy
  - ninja>=1.11.1
  - meson-python
  - pybind11
