# To use:
#
#   $ conda env create -f environment.yml  # can use `mamba` too
#   $ conda activate mkl-ext
#
name: mkl-ext
channels:
  - conda-forge
  - intel
dependencies:
  - python
  - dpctl
  - numpy
  - meson-python
  - ninja>=1.11.1
  - pybind11
  - pytest
